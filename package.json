{
  "name": "swift-injection-generator",
  "displayName": "Swift Injection Generator/Remover",
  "description": "Generate and remove InjectionNext boilerplate code in SwiftUI with one tap or a keyboard shortcut.",
  "version": "1.2.5",
  "publisher": "Pamungkas",
  "icon": "images/logo.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ajinumoto/swift-injection-generator.git"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Programming Languages",
    "Snippets",
    "Other"
  ],
  "activationEvents": [
    "onLanguage:swift"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "swift-injection.generate",
        "title": "Generate Swift Injection",
        "icon": "$(zap)"
      },
      {
        "command": "swift-injection.remove",
        "title": "Remove Swift Injection",
        "icon": "$(trash)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "swift-injection.generate",
          "when": "editorLangId == swift",
          "group": "navigation@1"
        },
        {
          "command": "swift-injection.remove",
          "when": "editorLangId == swift",
          "group": "navigation@2"
        }
      ]
    },
    "keybindings": [
      {
        "command": "swift-injection.generate",
        "key": "cmd+alt+i",
        "mac": "cmd+alt+i",
        "when": "editorLangId == swift"
      },
      {
        "command": "swift-injection.remove",
        "key": "cmd+alt+k",
        "mac": "cmd+alt+k",
        "when": "editorLangId == swift"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "clean": "rm -rf out *.vsix"
  },
  "devDependencies": {
    "@types/node": "^16.x.x",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.0.0"
  }
}
